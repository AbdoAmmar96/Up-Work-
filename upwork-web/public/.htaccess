# SPA routing للموقع العام (root) — أي مسار مش ملف حقيقي يرجع index.html
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # لا تتدخّل في الـ API ولا الدّاشبورد (مجلدات منفصلة لها معالجتها الخاصة)
  RewriteRule ^api(/.*)?$ - [L]
  RewriteRule ^dashboard(/.*)?$ - [L]

  # ملفات وفولدرات موجودة فعلاً تُخدّم زي ما هي
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # أي حاجة تانية → index.html (React Router)
  RewriteRule ^ index.html [L]
</IfModule>

# Caching للأصول الثابتة
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"
  ExpiresByType font/woff2 "access plus 1 year"
</IfModule>

# ضغط
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css application/javascript application/json image/svg+xml
</IfModule>
